// ---- Created with 3Dmigoto v1.3.16 on Tue Sep 02 15:46:07 2025

SamplerState g_sampler_s : register(s0);
SamplerState g_mlaa_sampler_s : register(s1);
Texture2D<float4> g_texture : register(t0);
Texture2D<float4> g_mlaa_texture : register(t1);

// 3Dmigoto declarations
#define cmp -

void main(
  float4 v0 : SV_POSITION0,
  float2 v1 : TEXCOORD0,
  float2 w1 : TEXCOORD1,
  float2 v2 : TEXCOORD2,
  float2 w2 : TEXCOORD3,
  float2 v3 : TEXCOORD4,
  out float4 o0 : SV_Target0)
{
  float4 r0,r1,r2,r3,r4,r5,r6;
  uint4 bitmask, uiDest;
  float4 fDest;

  r0.xyzw = g_texture.Sample(g_sampler_s, v1.xy).xyzw;
  // r1.xz = g_mlaa_texture.Sample(g_mlaa_sampler_s, v1.xy).xz;
  // r1.y = g_mlaa_texture.Sample(g_mlaa_sampler_s, w1.xy).y;
  // r1.w = g_mlaa_texture.Sample(g_mlaa_sampler_s, w2.xy).w;
  // r2.x = dot(r1.xyzw, float4(1,1,1,1));
  // r2.y = cmp(r2.x == 0.000000);
  // if (r2.y != 0) {
  //   o0.xyzw = r0.xyzw;
  //   return;
  // }
  // r3.xyzw = g_texture.Sample(g_sampler_s, w1.xy).xyzw;
  // r4.xyzw = g_texture.Sample(g_sampler_s, v2.xy).xyzw;
  // r5.xyzw = g_texture.Sample(g_sampler_s, w2.xy).xyzw;
  // r6.xyzw = g_texture.Sample(g_sampler_s, v3.xy).xyzw;
  // r2.x = 1 + -r2.x;
  // r3.xyzw = r3.xyzw * r1.yyyy;
  // r0.xyzw = r0.xyzw * r2.xxxx + r3.xyzw;
  // r0.xyzw = r4.xyzw * r1.xxxx + r0.xyzw;
  // r0.xyzw = r5.xyzw * r1.wwww + r0.xyzw;
  // o0.xyzw = r6.xyzw * r1.zzzz + r0.xyzw;

  // uint2 size;
  // g_texture.GetDimensions(size.x, size.y);
  // o0.xyzw = DLAAPixelShader(v1.xy, size);
  o0 = r0;

  return;
}